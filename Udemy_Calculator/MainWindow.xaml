<Window x:Class="Udemy_Calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Udemy_Calculator"
        mc:Ignorable="d"
        Title="Calculator"
        ResizeMode="NoResize"
        Width="300"
        Background="LightGray"
        Height="595">

    <Border Background="Black" BorderBrush="Red" BorderThickness="1" CornerRadius="25">
         <Canvas HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10 0 0 0">
          <!--Main Container Grid-->
    <Grid x:Name="MainGrid"
          Background="Black">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <!--Call the MenuSide User control-->
        <local:MenuSide_Control Grid.Row="0"
                                Margin="-7 0 0 0"
                                x:Name="UIMenuSide" />

        <!--Call the Display User control-->
        <local:Display_Control Grid.Row="1"
                               Background="White"
                               x:Name="UIDisplay"
                               BorderBrush="Gray"
                               BorderThickness="1"
                               HorizontalAlignment="Right"
                               HorizontalContentAlignment="Stretch"
                               Margin="0 0 0 5" />

        <!--Call the Calculator User control-->
        <local:Calculator_Control Grid.Row="2"
                                  x:Name="UICalculator" />
    </Grid>
    </Canvas>
    </Border>
   
  

</Window>
